def duplicate(array):
    duplicate = []
    temp=[]
    for number in array:
        if number not in temp:
            temp.append(number)
        elif:
            duplicate.append(number)
return duplicate

